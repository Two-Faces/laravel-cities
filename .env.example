# Laravel Cities Configuration Examples
# Add these to your .env file to customize the package behavior

# Geonames download URL (default: https://download.geonames.org/export/dump)
GEONAMES_URL=https://download.geonames.org/export/dump

# Storage path for geo files (relative to storage_path(), default: geo)
GEO_STORAGE_PATH=geo

# Database table name (default: geo)
GEO_TABLE_NAME=geo

# Default chunk size for batch processing (default: 1000)
GEO_CHUNK_SIZE=1000

